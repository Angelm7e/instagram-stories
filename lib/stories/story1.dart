import 'package:flutter/material.dart';

class MyStory1 extends StatelessWidget {
  // const MyStory1({super.key, required this.text});

  // final String text;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepPurpleAccent,
      body: Center(
        child:Column(),
      ),
    );
  }
}